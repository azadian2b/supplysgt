version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - if [ "$GENERATE_AWS_EXPORTS" = "true" ]; then node scripts/write-aws-exports.js; fi
    build:
      commands:
        - CI=true npm test -- --watchAll=false
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
